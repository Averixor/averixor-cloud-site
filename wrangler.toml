# Cloudflare Pages — output лише з dist/ (після npm run build у Dashboard).
# Build command задається в Dashboard, НЕ тут ([build] Pages не підтримує).

name = "averixor-cloud-site"
compatibility_date = "2026-06-21"
pages_build_output_dir = "dist"
