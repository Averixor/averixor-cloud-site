{
  "name": "averixor-cloud-site",
  "private": true,
  "scripts": {
    "build": "bash scripts/build-production.sh",
    "verify": "bash scripts/verify-site.sh",
    "ci": "npm run build && npm run verify && npm run test:e2e",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
